#!/usr/bin/perl
eval 'exec perl -S $0 ${1+"$@"}'
  if $running_under_some_shell;

# @(#)$About: set_icon - change the icon name of the current window $

$esc = chr(27);
$bel = chr(7);
print "${esc}]1;@ARGV${bel}";
