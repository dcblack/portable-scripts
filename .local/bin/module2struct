#!/usr/bin/env perl5
#
#$Info: module2struct - {:ONE_LINE_DESCRIPTION_HERE:}. $

require v5.6;
use strict;
use warnings;
use Cwd qw(abs_path getcwd);
use English;
use FindBin qw($RealBin $RealScript);
use File::Basename;
use FileHandle;
use FindBin qw($RealBin $RealScript);
use Getopt::Long;
use Pod::Usage;
STDOUT->autoflush(1);
STDERR->autoflush(1);

