About this share/ directory
===========================

Many tools install themselves under `/usr/local/share`. This directory is an alternative (i.e., `${HOME}/.local/share`) used by installers supported with the *portable-scripts* collection.
